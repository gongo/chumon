<nav class="navbar navbar-inverse navbar-static-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <span class="navbar-brand">Chumon</span>
    </div>

    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to 'Home', root_path %></li>
        <li><%= link_to '商品一覧', products_path %></li>
        <li><%= link_to '注文一覧', orders_path %></li>
        <% if !session[:cart].nil? && !session[:cart].empty? %>
          <li>
            <%= link_to content_tag(:span, 'レジに進む ' + session[:cart].size.to_s, class: 'badge'), new_order_path %>
          </li>
        <% end %>
        <% if session[:user_id].nil? %>
          <li><%= link_to 'Log in', new_sessions_path %></li>
        <% else %>
          <li><%= link_to 'Log out', sessions_path,
                  :method => 'delete',
                  data: { confirm: 'ログアウトしてもよろしいですか?'}  %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
